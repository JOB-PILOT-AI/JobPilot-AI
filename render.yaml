services:
  - type: web
    name: jobpilot-backend
    root: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_PATH
        value: ./node_modules
